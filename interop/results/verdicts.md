# §4.5 subsumption: warden vs tenuo

Generated by `go run ./interop`. Measurement, not a test: a disagreement
is a finding, not a failure. Verdicts are (parent, derived) pairs;
`permit` means the derived constraint is a valid attenuation of the parent.

48 cases: 46 agree, 2 disagree.

| category | cases | agree | disagree |
|---|---:|---:|---:|
| direction-probe | 4 | 4 | 0 |
| no-witness-cross-type | 9 | 9 | 0 |
| range-inclusivity | 9 | 9 | 0 |
| all-matching | 6 | 4 | 2 |
| not_one_of-vs-one_of | 4 | 4 | 0 |
| control | 16 | 16 | 0 |

## direction-probe

asserts the argument mapping; a wrong verdict here voids the run

| case | warden | tenuo | | why this pair is here |
|---|---|---|---|---|
| `probe-range-narrow` | permit | permit |  | derived [0,10] inside parent [0,100] |
| `probe-range-widen` | deny | deny |  | probe-range-narrow swapped; must not permit |
| `probe-oneof-to-exact` | permit | permit |  | §4.5 exact: parent one_of listing the value |
| `probe-exact-to-oneof` | deny | deny |  | probe-oneof-to-exact swapped; (exact, one_of) is undeclared |

## no-witness-cross-type

§4.5 declares no rule; warden rejects by absence. no witness distinguishes the two

| case | warden | tenuo | | why this pair is here |
|---|---|---|---|---|
| `nowit-range-to-oneof` | deny | deny |  | derived {1,2,5} all inside parent [0,10] |
| `nowit-oneof-to-range` | deny | deny |  | derived [1,1] admits exactly 1, which parent lists |
| `nowit-oneof-to-any` | deny | deny |  | semantically identical to the parent |
| `nowit-any-to-oneof` | deny | deny |  | nowit-oneof-to-any in the other direction; also identical |
| `nowit-any-to-exact` | deny | deny |  | derived admits exactly what the parent's only clause admits |
| `nowit-all-to-range` | deny | deny |  | parent conjunction is [0,10]; derived is [0,10] |
| `nowit-range-to-all` | deny | deny |  | derived conjunction is [0,5], inside parent [0,10] |
| `nowit-notoneof-to-oneof` | deny | deny |  | derived {1,2} avoids the parent's only exclusion |
| `nowit-notoneof-to-exact` | deny | deny |  | derived "y" is not the parent's excluded "x" |

## range-inclusivity

inclusivity at an equal bound and missing bounds, where the natural reading is wrong

| case | warden | tenuo | | why this pair is here |
|---|---|---|---|---|
| `range-derived-exclusive-tighter` | permit | permit |  | §4.5: derived exclusive against parent inclusive at an equal bound is tighter |
| `range-derived-inclusive-wider` | deny | deny |  | the reverse admits the endpoint the parent excluded |
| `range-derived-exclusive-max-tighter` | permit | permit |  | the same rule on the max bound |
| `range-derived-inclusive-max-wider` | deny | deny |  | max bound, reverse direction |
| `range-derived-drops-max` | deny | deny |  | §4.5: a missing derived bound is valid only if the parent's is missing too |
| `range-derived-drops-min` | deny | deny |  | the same on the min side |
| `range-derived-adds-min` | permit | permit |  | parent unbounded below; adding a bound is a restriction |
| `range-both-unbounded-min` | permit | permit |  | both missing on the same side is valid |
| `range-exclusive-both-equal` | permit | permit |  | equal bounds, equal inclusivity, nothing tightened or widened |

## all-matching

§4.5's one-to-one clause assignment, including a greedy dead-end

| case | warden | tenuo | | why this pair is here |
|---|---|---|---|---|
| `all-clause-reuse-two-parents-one-derived` | deny | permit | **DIFFERS** | §4.5: a single derived clause MUST NOT satisfy more than one parent clause |
| `all-clause-reuse-wildcards` | deny | permit | **DIFFERS** | the same rule, minimal shape: two parent clauses, one derived clause |
| `all-greedy-dead-end` | permit | permit |  | greedy assignment strands parent one_of[1]; only backtracking finds the matching |
| `all-parent-clause-dropped` | deny | deny |  | a parent clause with no derived match at all; both must deny |
| `all-derived-adds-clauses` | permit | permit |  | §4.5: unmatched additional derived clauses are permitted |
| `all-one-to-one-available` | permit | permit |  | a genuine one-to-one assignment exists; no reuse needed |
`all-greedy-dead-end` agrees for a different reason on each side: warden needs an augmenting path to find the matching, while tenuo has no one-to-one requirement to dead-end against. Agreement on that row is not evidence of matching parity; the two reuse rows are what separates them.

## not_one_of-vs-one_of

the one cross-type pair §4.5 invalidates in prose

| case | warden | tenuo | | why this pair is here |
|---|---|---|---|---|
| `notoneof-against-parent-oneof` | deny | deny |  | §4.5 one_of: enforcement points MUST reject this cross-type pair |
| `notoneof-excluding-parent-member` | deny | deny |  | still invalid even when the exclusion looks like a narrowing |
| `notoneof-adds-exclusion` | permit | permit |  | §4.5 not_one_of: derived excluded set is a superset |
| `notoneof-removes-exclusion` | deny | deny |  | dropping an exclusion admits what the parent denied |

## control

rules §4.5 states plainly. reported separately so agreement is not carried by easy rows

| case | warden | tenuo | | why this pair is here |
|---|---|---|---|---|
| `ctl-wildcard-parent-to-exact` | permit | permit |  | any type subsumes a parent wildcard |
| `ctl-derived-wildcard-widens` | deny | deny |  | a derived wildcard is valid only against a parent wildcard |
| `ctl-wildcard-to-wildcard` | permit | permit |  | the one permitted derived wildcard |
| `ctl-oneof-shrinks` | permit | permit |  | derived value set is a subset |
| `ctl-oneof-grows` | deny | deny |  | derived value set adds a member |
| `ctl-contains-requires-more` | permit | permit |  | §4.5 contains: derived required set is a superset |
| `ctl-contains-requires-less` | deny | deny |  | removing a required element widens |
| `ctl-subset-shrinks` | permit | permit |  | §4.5 subset: derived allowed set is a subset |
| `ctl-subset-grows` | deny | deny |  | adding an allowed element widens |
| `ctl-any-drops-clause` | permit | permit |  | §4.5 any, the draft's own worked example |
| `ctl-any-adds-clause` | deny | deny |  | §4.5 any, the draft's own counter-example |
| `ctl-any-cross-type-clause` | permit | permit |  | §4.5 any: cross-type subsumption between clauses is permitted |
| `ctl-exact-same-value` | permit | permit |  | §4.5 exact against an equal parent exact |
| `ctl-exact-different-value` | deny | deny |  | a different value is not subsumed |
| `ctl-range-to-exact-inside` | permit | permit |  | §4.5 exact against a parent range containing the value |
| `ctl-range-to-exact-outside` | deny | deny |  | the value falls outside the parent interval |

